<Window x:Class="OpenScreenRecorder.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenScreen Recorder"
        Height="150" Width="480"
        MinHeight="150" MinWidth="480"
        Background="#0f1115"
        Foreground="#f5f7fb">
  <Window.Resources>
    <SolidColorBrush x:Key="CardBrush" Color="#161a22"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#38b6ff"/>
    <SolidColorBrush x:Key="ComboBg" Color="#1f2532"/>
    <SolidColorBrush x:Key="ComboBorder" Color="#2d3342"/>
    <SolidColorBrush x:Key="ComboHoverBorder" Color="#4a5670"/>
    <SolidColorBrush x:Key="ComboHighlightBg" Color="#2b78c5"/>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="#f5f7fb" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="#0f1115"/>
      <Setter Property="Background" Value="#38b6ff"/>
      <Setter Property="BorderBrush" Value="#38b6ff"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
             <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
             </Border>
             <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                   <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                   <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
             </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value> 
      </Setter>
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Background" Value="{StaticResource ComboBg}"/>
      <Setter Property="Foreground" Value="#f5f7fb"/>
      <Setter Property="BorderBrush" Value="{StaticResource ComboBorder}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,4"/>
      <Setter Property="ItemContainerStyle">
        <Setter.Value>
          <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#f5f7fb"/>
            <Setter Property="Background" Value="{StaticResource ComboBg}"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                  <Border x:Name="ItemBorder"
                          Background="{TemplateBinding Background}"
                          SnapsToDevicePixels="True">
                    <ContentPresenter />
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                      <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource ComboHighlightBg}"/>
                      <Setter Property="Foreground" Value="#ffffff"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                      <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource ComboHighlightBg}"/>
                      <Setter Property="Foreground" Value="#ffffff"/>
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </Setter.Value>
      </Setter>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid SnapsToDevicePixels="True">
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="36" />
              </Grid.ColumnDefinitions>

              <Border x:Name="OuterBorder"
                      Grid.ColumnSpan="2"
                      CornerRadius="4"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"/>

              <ContentPresenter x:Name="ContentSite"
                                Margin="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                RecognizesAccessKey="True"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>

              <ToggleButton x:Name="ToggleButton"
                            Grid.Column="1"
                            Focusable="False"
                            ClickMode="Press"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            Background="Transparent"
                            BorderBrush="{x:Null}">
                <Grid Width="36" Height="32">
                  <Path Data="M 0 0 L 4 4 L 8 0 Z"
                        Fill="#c7d4ee"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
                </Grid>
              </ToggleButton>

              <Popup x:Name="Popup"
                     Placement="Bottom"
                     IsOpen="{TemplateBinding IsDropDownOpen}"
                     AllowsTransparency="True"
                     Focusable="False"
                     PopupAnimation="Slide">
                <Border x:Name="DropDownBorder"
                        Margin="0,2,0,0"
                        CornerRadius="6"
                        Background="{StaticResource ComboBg}"
                        BorderBrush="{StaticResource ComboBorder}"
                        BorderThickness="1"
                        SnapsToDevicePixels="True">
                  <ScrollViewer Margin="0"
                                SnapsToDevicePixels="True"
                                CanContentScroll="True">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource ComboHoverBorder}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="#dbe5ff"/>
      <Setter Property="Margin" Value="0,0,12,0"/>
    </Style>
  </Window.Resources>

  <Grid Margin="12">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Top Row: Title + Timer -->
    <Grid Grid.Row="0" Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="OpenScreen Recorder" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Foreground="#9fb1d1"/>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
             <TextBlock x:Name="StatusText" Text="Ready" Foreground="#6f809c" Margin="0,0,10,0" VerticalAlignment="Center"/>
             <Border Background="#1f2532" CornerRadius="4" Padding="8,2">
                 <TextBlock x:Name="TimerText" Text="00:00:00" FontFamily="Consolas, Monospace" FontWeight="Bold" Foreground="#38b6ff"/>
             </Border>
        </StackPanel>
    </Grid>

    <!-- Middle Row: Controls -->
    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" /> <!-- Source -->
            <ColumnDefinition Width="Auto" /> <!-- Audio -->
            <ColumnDefinition Width="Auto" /> <!-- Record Button -->
        </Grid.ColumnDefinitions>
        
        <ComboBox x:Name="SourceCombo" Grid.Column="0" Height="36" Margin="0,0,8,0" VerticalContentAlignment="Center" />

        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,8,0">
             <CheckBox x:Name="SystemAudioCheck" Content="System Audio" IsChecked="True" FontSize="11"/>
             <CheckBox x:Name="MicAudioCheck" Content="Microphone" IsChecked="True" FontSize="11" Margin="0"/>
        </StackPanel>
        
        <Button x:Name="RecordButton" Grid.Column="2" Content="Record" Width="80" Height="36" Click="Record_Click" Background="#ff4d6d" BorderBrush="#ff4d6d" Foreground="White" />
    </Grid>

    <!-- Bottom Row: Post-recording actions (Hidden by default or valid when not empty) -->
    <Border x:Name="ResultPanel" Grid.Row="2" Margin="0,8,0,0" Background="#1f2532" CornerRadius="4" Padding="8" Visibility="Collapsed">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Saved." VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <Button Content="Open File" Click="OpenFile_Click" Background="Transparent" BorderBrush="Transparent" Foreground="#38b6ff" Padding="4,2"/>
            <Button Content="Show in Folder" Click="OpenFolder_Click" Background="Transparent" BorderBrush="Transparent" Foreground="#38b6ff" Padding="4,2"/>
        </StackPanel>
    </Border>

  </Grid>
</Window>
